### zig ###
# Zig programming language

.zig-cache/
zig-out/
build/
build-*/
docgen_tmp/

# Godot editor cache. Nothing here is committed: `zig build test-godot`
# regenerates the uid and scene-id caches from the fixture project on every run.
.godot/

### os ###
.DS_Store
._*
Thumbs.db

### editors ###
.idea/
*.swp

# Skills are authored in skills/ and copied out by install.sh
.cursor/skills/

# Backups left behind if a batch run is interrupted
*.godot-cli-batch-backup
