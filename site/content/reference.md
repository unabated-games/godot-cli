---
title: godot-cli command reference
description: Every godot-cli command, option, and exit code, generated from the binary's own command tree.
include: docs/commands.md
---

# Command reference

This page is generated from the `CommandSpec` tree inside godot-cli by `zig build docs`, and CI fails if it drifts from the binary. The same content is available offline as `man godot-cli`, and as data through `godot-cli reference --format json`.
